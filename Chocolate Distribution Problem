sort(a.begin(),a.end());
        long long mini = INT_MAX;
        long long l=0, r=m-1;
        while(r<n){
            long long diff = a[r]-a[l];
            mini = min(mini,diff);
            l++; r++;
        }
        return mini;
